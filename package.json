{
  "name": "innovation-day-rater-web",
  "version": "0.0.1",
  "description": "Web client for Xebia Innovation Day Rating App",
  "main": "src/js/app.js",
  "author": "Steven Mulder",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/smuldr/innovation-day-rater-web.git"
  },
  "dependencies": {
    "async": "^1.3.0",
    "classnames": "^2.1.2",
    "cordova-dialogs": "^1.0.2",
    "httpify": "^1.0.0",
    "react": "^0.13.3",
    "react-container": "^0.2.2",
    "react-sentry": "^0.4.1",
    "react-tappable": "^0.5.2",
    "react-timers": "^4.0.0",
    "touchstonejs": "^0.5.0"
  },
  "devDependencies": {
    "gulp": "^3.9.0",
    "happiness": "*",
    "touchstonejs-tasks": "*"
  },
  "scripts": {
    "build": "gulp clean && gulp build",
    "build:production": "gulp clean && NODE_ENV=production gulp build",
    "lint": "happiness",
    "prepare": "gulp clean && gulp prepare",
    "prepare:production": "gulp clean && NODE_ENV=production gulp prepare",
    "test": "npm run lint",
    "start": "gulp clean && gulp dev",
    "watch": "gulp clean && gulp watch"
  },
  "happiness": {
    "ignore": [
      "platforms",
      "plugins",
      "src/data",
      "www"
    ]
  }
}
